# LinkCage host dependencies (pin new deps here).

# CA bundle for TLS verification. macOS's python.org Python has no system CA
# store, so the URLhaus feed / Safe Browsing HTTPS fetches fail without this.
certifi
